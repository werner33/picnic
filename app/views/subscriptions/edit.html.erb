<div class="container">
  <div class="col-md-4 col-md-offset-4">

<h1>Change Subscription</h1>

<%= form_for @subscription, :html => {:multipart => true} do |f| %>
  <% if @subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subscription.errors.count, "error") %> prohibited this picnic from being saved:</h2>

      <ul>
      <% @subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.label :email %></br>
  <%= f.text_field :email, class: "form-field" %></br>
  
  <div class="actions">
    <%= f.submit "Submit", class: "perfect-red-button" %>
  </div>
<% end %>


<%= link_to 'Unsubscribe', subscription_path(@subscription),  method: :delete, 
              data: {confirm: 'Are you sure?' } %> 

  </div>
</div>  

